import {describe, it, expect, beforeEachProviders, inject} from 'angular2/testing';
import {TodayApp} from '../app/today';

beforeEachProviders(() => [TodayApp]);

describe('App: Today', () => {
});

